export const sendVerificationCode = (email, code) => {
  // Implement the logic to send the verification code to the user's email
  console.log(`Sending verification code ${code} to email ${email}`);
};
